# YouTube Stream Dashboard (HTML + GitHub Actions)

1. Tambahkan `index.html` ke GitHub Pages.
2. Tambahkan `.github/workflows/stream.yml`.
3. Buat secret repo `YT_STREAM_KEY`.
4. Buat GitHub PAT dengan scope `workflow`.
5. Buka `index.html` dan trigger streaming.
